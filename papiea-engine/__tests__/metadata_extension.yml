OwnerTenant:
  type: object
  title: specifies tenant & owner
  description: specifies tenant & owner
  required:
    - owner
    - tenant_id
  properties:
    owner:
      type: string
    tenant_id:
      type: string