import {load} from "js-yaml";
import {Provider} from "./typescript_sdk_interface";
import {Data_Description} from "../core";
import {Kind} from "../papiea";

export class ProviderSdk implements Provider {
    new_kind(entity_yaml: Data_Description): Kind {

    }
}